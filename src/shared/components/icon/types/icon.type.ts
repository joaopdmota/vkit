type IconType = {
  animation?: string
  solo?: boolean
  color?: 'danger' | 'info' | 'light' | 'success' | 'warning' | 'default'
  infinite?: string
  name: string
  size?: number
}

export default IconType
