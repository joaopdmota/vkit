import { ReactElement } from 'react'

import ButtonType from '../../shared/components/button/types/button.type'

declare function Button({ ...props }: ButtonType): ReactElement | null

export default Button
