import { ReactElement } from 'react'

import ScrollbarType from '../../shared/components/context/scrollbar/types/scrollbar.type'

declare function Scrollbar({ ...props }: ScrollbarType): ReactElement | null

export default Scrollbar
