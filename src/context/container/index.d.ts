import { ReactElement } from 'react'

import ContainerType from '../../shared/components/context/container/types/container.type'

declare function Container({ ...props }: ContainerType): ReactElement | null

export default Container
