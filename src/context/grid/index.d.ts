import { ReactElement } from 'react'

import GridType from '../../shared/components/context/grid/types/grid.type'

declare function Grid({ ...props }: GridType): ReactElement | null

export default Grid
